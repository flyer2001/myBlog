name: PublishSwift

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Navigate to parent directory
        working-directory: '**/myBlog'
        run: pwd

