name: PublishSwift

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Navigate to parent directory
        run: cd ..
      - name: Navigate to parent directory
        run: cd ..
      - name: Navigate to parent directory
        run: cd ..
      - name: Navigate to parent directory
        run: cd ..

      - name: Print folders on current level
        run: ls
