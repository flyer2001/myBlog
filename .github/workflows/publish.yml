name: PublishSwift

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        run: echo "$(dirname $(dirname $(dirname $(dirname $(dirname ${GITHUB_WORKSPACE})))))"

